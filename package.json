{
  "name": "@5seg/tana",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:init:local": "wrangler d1 execute tana-db --local --file=schema.sql",
    "d1:init:remote": "wrangler d1 execute tana-db --remote --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260916.1",
    "@types/bun": "latest",
    "wrangler": "^4.132.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "hono": "^4.13.8"
  }
}
